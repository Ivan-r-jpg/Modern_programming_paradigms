#pragma once
#include <iostream>
#include <string>
#include <limits>

int inputInteger(const int& min, const int& max, const std::string& text); // Прототип функції для вводу цілого числа з перевіркою допустимого діапазону значень 

double inputDouble(const double& min, const double& max, const std::string& text); // Прототип функції для вводу числа з плаваючою точкою з перевіркою допустимого діапазону значень

